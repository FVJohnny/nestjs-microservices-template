{
  "name": "nestjs-postgresql",
  "sourceRoot": "backend/libs/postgresql/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build",
        "cwd": "backend/libs/postgresql"
      },
      "dependsOn": ["^build"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "backend/libs/postgresql/src/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "backend/libs/postgresql/jest.config.js"
      }
    }
  },
  "tags": [
    "type:lib",
    "scope:nestjs",
    "topic:postgresql"
  ],
  "implicitDependencies": ["nestjs-common"]
}