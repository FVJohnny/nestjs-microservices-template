{
  "name": "nestjs-redis",
  "sourceRoot": "backend/libs/redis/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npm run build",
        "cwd": "backend/libs/redis"
      },
      "dependsOn": ["^build"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["backend/libs/redis/src/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "backend/libs/redis/jest.config.js"
      }
    }
  },
  "tags": ["type:lib", "scope:nestjs", "topic:redis"],
  "implicitDependencies": ["nestjs-common"]
}
